import { TemplateDefinition } from "../types";

// ============================================================================
// ALL TEMPLATES - NOW FETCHED DYNAMICALLY FROM API
// ============================================================================

/**
 * @deprecated Templates are now fetched dynamically from the API.
 * Use templateStore to load templates instead.
 */
export const ALL_TEMPLATES: TemplateDefinition[] = [];
