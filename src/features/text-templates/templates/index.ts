import { TemplateDefinition } from "../types";

// ============================================================================
// ALL TEMPLATES COMMENTED OUT - STARTING FROM SCRATCH
// ============================================================================
// Uncomment imports and add to array as needed

/*
import broadcastTicker from './broadcast-ticker.meta';
import cinematicOpener from './cinematic-opener.meta';
import cleanLowerThird from './clean-lower-third.meta';
import documentaryCard from './documentary-card.meta';
import glitchTitle from './glitch-title.meta';
import kineticWordReveal from './kinetic-word-reveal.meta';
import minimalFadeTitle from './minimal-fade-title.meta';
import neonTitleSlam from './neon-title-slam.meta';
import quoteCard from './quote-card.meta';
import socialCaptionPop from './social-caption-pop.meta';
import splitReveal from './split-reveal.meta';
import statCallout from './stat-callout.meta';
import typewriter from './typewriter.meta';
import verticalShortTitle from './vertical-short-title.meta';
import youtubeEndcard from './youtube-endcard.meta';
*/

export const ALL_TEMPLATES: TemplateDefinition[] = [
  // broadcastTicker,
  // cinematicOpener,
  // cleanLowerThird,
  // documentaryCard,
  // glitchTitle,
  // kineticWordReveal,
  // minimalFadeTitle,
  // neonTitleSlam,
  // quoteCard,
  // socialCaptionPop,
  // splitReveal,
  // statCallout,
  // typewriter,
  // verticalShortTitle,
  // youtubeEndcard
];
